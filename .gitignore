.Rhistory
.RData
.Rproj.user/
*.aux
*.log
*.out
.DS_Store

# Raw source data — reproducible from the Census, never committed.
# Cbp02co.txt is 157 MB; GitHub hard-rejects blobs over 100 MB, so committing
# this would fail the push, not merely bloat the repo. Download URL is recorded
# in causal-did/EXPOSURE_DEFECT_2026-08-12.md.
causal-did/raw/
raw/CBP/
*.zip

# --- LaTeX build artifacts. Added August 12, 2026 during assembly. *.aux, *.log
# and *.out were already here; the bibtex pair was not, and `git add -A` would
# have swept them in alongside the first real compile.
*.bbl
*.blg
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
